{
  "name": "talent-iq",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "version-check": "node --version && npm --version",
    "build": "npm run version-check && npm ci --prefix backend && npm ci --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}